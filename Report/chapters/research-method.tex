\subsection{Experimental Design}\label{subsec:experimental-design}
Two different visual programming languages were taught to a group of middle-school students to evaluate which of the visual programming languages would be effective at conveying different programming design principles to young learners.

The programming languages were compared through A/B testing by conducting a two-day workshop.
Group 1 was taught using Scratch, while group 2 was taught using Godot Orchestrator.
Both languages are incorporated into game engines by the developers, meaning that we could use game development to teach software engineering.
Students in each group were asked to build a game using the language, which incorporated the following software development concepts:

\begin{itemize}
	\item Instantiation
	\item Modularity
	\item State Management
	\item Event Handling
\end{itemize}

These concepts were chosen as they serve as ways to introduce topics commonly taught during the first year of Software engineering at university, whilst also being commonplace in game development.
This way, the students would be able to use them in their own projects, eventually increasing motivation.
The chosen concepts were more complex than basic programming concepts, like variables or loops.
This was due to the research goal, which was to investigate how different visual programming languages convey different software design principles.

Before and after the workshop, the students were asked a set of questions to determine their knowledge of software design concepts.
This was done through a survey and a multiple choice test.
Finally, we evaluated the code created by the students to see how well they applied the different programming concepts, based on whether they used a block-based (Scratch) or a node-based visual programming language (Godot Orchestrator).

The study was conducted in cooperation with Teknologiskolen, which is an association that teaches students between six and 18 years old in programming or other technology-related topics.
Participants were recruited from Teknologiskolen's pool of students, meaning that it was likely that participants would have some programming experience.
In response to our digital invitations, three students participated in the workshop.
All participants were male and aged between 12 and 13.
The low participation left us with two groups, one group of one student working with Scratch and another group consisting of two students working with Godot Orchestrator.
Students were assigned randomly to one of the groups.

\subsection{Language Choice}\label{subsec:language-choice}

\subsubsection{Candidates}
Several visual programming languages were evaluated before the choice landed on Scratch and the plugin Orchestrator \cite{orchestrator_docs_2026}.
Unreal Blueprints~\cite{epicgames_blueprints_2025} was a strong candidate, as it is popularly used to develop AAA games by large studios, thus indicating professional relevance.
However, we could not expect students to bring computer performant enough to run Unreal Engine; it was therefore discarded.
There are also two Roblox\footnote{\url{https://www.roblox.com/}} engine plugins that enables visual programming, that enables block-based and node-based programming.
These plugins are behind a paywall and were not chosen because of that, and that would create a barrier of entry that other options does not have.
Makecode\footnote{{\url{https://makecode.microbit.org/}}}was also considered, but was dropped for Scratch rather quickly.
Scratch and Makecode both do the block-based visual programming, but Scratch is game focused where Makecode is more focused on Micro:bit.

The two visual programming languages chosen are Scratch and the orchestrator plugin for the game engine Godot.
The reason for these candidate to be chosen was because of their similarities, both are free to use, capable to make games, and do not have big hardware requirements.
And they cover two different visual programming paradigms.
Furthermore, Orchestrator is a node-based visual programming language with a UI similar to Unreal's Blueprints, a proven, viable approach for game development, and Scratch remains the state of the art for teaching programming with visual scripting.

Using the criteria defined by Mannila and Raadt~\cite{mannila_objective_2006} the two languages will be compared for their ability to introduce programming to novices.

\subsubsection{Scratch} is an educational programming environment designed to introduce young learners to fundamental programming concepts.
It uses a block-based visual programming paradigm, where users construct logic by snapping together graphical blocks instead of writing textual code~\cite{resnick_scratch_2009}.

\paragraph{Suitable for teaching}
Scratch was designed to be used for teaching.
Scratch has colorful categories that create a clear separation of blocks functionality, this does require the user to know where to look for a certain type of block.
The interface was designed to be simple to navigate, there is no need/option to get lost in engine folders and menus.
No setup needed, users can start making a game right away, there even is no need to provide your own sprites, scratch provides a library of them.
The block is “drag and drop” and the functionality of the block which is shown by their name is to the letter.
The design of the block also shows “where” they can be placed with their notches and shapes.
Scratch requires a relative low reading comprehension level to be usable.

\paragraph{Can be used to apply physical analogies}
Scratch fulfills tthis condition with its ability to make games.

\paragraph{Offers a general framework}
Learning the programming fundamentals is possible with Scratch.
Scratch provides a framework for learning, it is what Scratch is trying to push.

\paragraph{Promotes a design driven approach for teaching software}
Scratch with its drag and drop blocks promotes experimentation, prototyping and iteration.
Users will focus on structure and interaction between blocks, and can get rapid feedback on their code.

\paragraph{Is interactive and facilitates rapid code development}
Scratch is highly interactive, the users will need to use blocks from most categories to make a game.
Users have the ability to drag a block of code out of their current coding window and place it in the Scratch backpack.
It can be used as a place to save progress or prototypes without getting a cluttered coding window.
And with ability to drag these saved code blocks out like from the normal categories, code development can be very rapid.

\paragraph{Provides a seamless development environment}
Scratch comes with its own integrated development environment, where everything is in one place, the code, sprite, background and sound editor are available to the user.

\paragraph{Has a supportive user community}
A strong community provides tutorials, documentation, forums, and help resources, which are essential for beginners who need assistance outside the classroom.
Scratch has a website the community can share the games, and for others to play.
On the same website users can use the Scratch forum to ask questions and get help.
There is also tutorials for many aspects of Scratch on the website.

\paragraph{Is consistently supported across environments}
Scratch is mainly a web-based application, so it is supported across platforms that support a web browser.

\paragraph{Is freely and easily available}
Scratch is free to use and there are no technical barriers to use it.
Scratch runs in the browsers, so having a browser is the only requirement.

\paragraph{Is supported with good teaching material}
There is a lot of resources for self-study on the Scratch website, and in general on the internet.
There is also a section on the Scratch website for educators, with the resources and curriculums guides for teaching.

\paragraph{Not only used in education}
Scratch is not relevant in industry or real-world applications.
But there are a community of users that use Scratch to makes games beyond an educational scope.

\paragraph{Concept Coverage}
Scratch can be used to teach following programming concepts


\begin{itemize}
	\item Modularity/Functions - There is the block category called "Custom blocks" where users have the ability to create custom block/function.
	\item Instantiation - Instantiation can be done with the "clone" blocks. There is a block for creating the functionality when a sprite starts as a clone. And there is a block to create a clone of a sprite. Any sprite can create a clone of another sprite.
	\item Event handling - Events and their handling is done with the blocks “Broadcast” and “Receive”. Broadcast sends a message out to anyone listing for it, and will execute code on receive. Other kind of events can happen and trigger on other blocks like “touching”, a way to execute code when two sprites are colliding
	\item State management - Statemanagement is possible in Stracth with right combination of blocks.
\end{itemize}

\subsubsection{Godot Orchestrator}
Godot Orchestrator is a node-based visual scripting language designed for seamless integration into Godot's workflow.
Rather than writing code line-by-line, users connect nodes visually to represent logic flow, making program structure immediately visible.
It was built to ensure that anything implementable in GDScript or C\# is also implementable within Orchestrator itself, providing feature parity with Godot's traditional scripting languages.

\paragraph{Suitable for teaching}

In contrast to other languages like Scratch, Orchestrator was not designed as an education tool but rather as a full industry language since it is a plugin for a professional grade game engine.

Learning Orchestrator therefore requires a foundational understanding of variables, dataflow, and functions/methods, but it remains reasonably accessible for learners with these prerequisites.
The visual nature of node-based scripting reduces syntax barriers, learners focus on logic and structure rather than memorizing language conventions.

However, grasping how data flows between nodes and understanding function parameters is essential before comfortable progression.
The main drawback of Orchestrator is the same drawback which can be found in all visual scripting languages: when the code becomes complex, it can be visually cluttered.
Deeply nested logic or large state machines become harder to read and debug compared to traditional text-based code, potentially lengthening development time for intricate systems.

The user interface, while familiar and easy to use for those with experience in other node-based languages like Unreal's Blueprints or the general Godot workflow, can be overwhelming for people unfamiliar with these tools.

\paragraph{Can be used to apply physical analogies}
Godot is a powerful engine with extensive creative possibilities.
It enables learners to create fully functional, even commercially viable games.
Its 2D and 3D capabilities, physics engine, and animation tools allow for rapid prototyping.

The ability to visually simulate real-world interactions, such as physics-based movement or object interaction, allows learners to apply programming concepts through meaningful analogies.

The amount of freedom should allow for a high amount of motivation since there is full creative freedom.

\paragraph{Offers a general framework}

As part of a full-featured game engine, Orchestrator supports a wide range of programming concepts.
For instance, Godot provides built-in support for patterns such as the observer pattern, allowing learners to explore event-driven architectures.

Furthermore, the environment enables the use of functions, classes, and other object-oriented programming constructs, supporting the development of a general and transferable understanding of software design principles.

This supports the development of transferable programming knowledge by exposing learners to concepts that extend beyond the visual environment.

\paragraph{Promotes a design driven approach for teaching software}

Orchestrator partially supports a design-driven approach by representing programs as interconnected nodes within a graphical environment.
This structure makes the flow of execution explicit and allows learners to reason about program behavior in terms of control flow and interactions between components.
In this sense, it supports thinking about problem structure rather than focusing solely on individual instructions.

Furthermore, as part of the Godot game engine, Orchestrator is embedded within a broader development environment that includes tools, libraries, and assets.
This allows learners to engage with elements of the software development process beyond simple implementation, such as prototyping and system integration.

However, Orchestrator does not explicitly enforce design principles or provide a structured methodology for software development.
The flexibility of the node-based approach allows learners to construct programs in an unstructured manner, which may result in complex or poorly organized graphs.
As such, while Orchestrator facilitates aspects of design-oriented thinking, it does not fully promote a comprehensive design-driven methodology for teaching software.

\paragraph{Is interactive and facilitates rapid code development}

Orchestrator, as a visual scripting system integrated within a game engine, enables code to be tested immediately within the game environment.
This provides instant visual feedback, allowing learners to quickly observe the effects of their changes and iterate on their solutions.
Such interactivity supports rapid prototyping and encourages experimentation.

However, the development process is inherently tied to a game or scene context.
This means that implementing and testing new ideas typically requires at least a partial setup of a larger application, rather than allowing completely isolated experimentation.
As a result, while Orchestrator provides strong interactive feedback and supports rapid development, it only partially fulfills the criterion, as it does not fully support testing small program fragments independently of a broader context.

\paragraph{Provides a seamless development environment}
Orchestrator benefits from its integration within the Godot game engine, providing a unified environment for scripting, asset management, and testing.
Learners can write, test, and debug code within the same interface, reducing the need to switch between tools and supporting a smooth development workflow.
This integration simplifies the development process and allows learners to focus on problem-solving rather than tool configuration.

\paragraph{Has a supportive user community}
Godot has an active and growing open-source community that provides extensive documentation, tutorials, and discussion forums.
While the Orchestrator plugin itself has a smaller and less mature community, learners can still benefit from the broader Godot ecosystem, as many concepts and workflows are shared across scripting approaches.

\paragraph{Is consistently supported across environments}

Godot, the engine that Orchestrator is a plugin for, is available on a variety of platforms.
Therefore, Orchestrator is also available on all platforms.

\paragraph{Is freely and easily available}

Godot is a free game engine, and the plugin orchestrator is easy to install from within the engine.

\paragraph{Is supported with good teaching material}

Godot receives regular updates (version 4.6.2 at the time of writing), and has an array of teaching materials specific to the engine.
However, Orchestrator is newer and has a less mature ecosystem than tools like Unreal’s Blueprints, resulting in fewer tutorials.
It shares built-in functionality with Godot’s textual scripting language, allowing many existing tutorials to be reused.

\paragraph{Not only used in education}

It is not primarily designed for education, but rather for professional game development.
However, its features make it suitable for educational use in certain contexts.

\paragraph{Concept Coverage}
As a language designed for a full game engine, Orchestrator can implement all the concepts we plan to teach in this workshop, though some may be moderately complex to use.


\begin{itemize}

	\item \textbf{Modularity – Organizing reusable logic into callable blocks}
	      Orchestrator treats functions as first-class node graphs.
	      Learners create custom function nodes, define input parameters as visual pins, and construct logic by connecting nodes.
	      This representation makes relationships between inputs, logic, and outputs clear.
	      However, function overloading is not supported, and recursive functions can be difficult to visualize.
	\item \textbf{Instantiation – Creating and spawning game objects dynamically within scenes}
	      Orchestrator handles instantiation through dedicated nodes that load and create scene instances at runtime.
	      Learners connect an \texttt{Instantiate Scene} node, specify the scene path, optionally pass constructor parameters through input pins, and receive the instantiated object as output.
	      While this makes the concept of instantiation visible, the underlying process remains abstracted.
	      The node-based approach makes the creation process explicit and visual, but complex patterns such as object pooling or batched instantiation can become verbose.

	\item \textbf{Event Handling – Responding to input, collisions, and signals}
	      Orchestrator integrates with Godot’s signal system through event nodes.
	      When a signal is emitted (e.g., input or collision), a corresponding node is triggered.
	      Learners connect input nodes (e.g., \texttt{Input Action Pressed}) directly to logic, making cause-and-effect relationships clear.
	      However, handling multiple overlapping events can result in complex and difficult-to-read node graphs.

	\item \textbf{State Management – Tracking and updating object properties}
	      State is managed through member variables that persist within the script node.
	      Learners use \texttt{Set Property} and \texttt{Get Property} nodes to modify and access values.
	      The system supports various data types, including primitives, vectors, arrays, and dictionaries.
	      State changes are explicit, improving traceability, but can lead to large and complex node graphs when many variables are involved.
	      State machines can be implemented using boolean variables, similar to textual programming languages.


\end{itemize}

\subsection{Comparison of languages}\label{subsec:comparison-of-languages}


\begin{table}[!htbp]
	\centering
	\resizebox{\columnwidth}{!}{%
		\begin{tabular}{|p{7cm}|c|c|}
			\hline
			\textbf{Criteria}                                  & \textbf{Scratch} & \textbf{Godot Orchestrator} \\
			\hline
			Suitable for teaching                              & X                &                             \\
			Can be used to apply physical analogies            & X                & X                           \\
			Offers a general framework                         & X                & X                           \\
			Promotes a design-driven approach                  &                  & X                           \\
			\hline
			Interactive and facilitates rapid code development & X                & X                           \\
			Provides a seamless development environment        & X                & X                           \\
			\hline
			Has a supportive user community                    & X                & X                           \\
			Is consistently supported across environments      & X                & X                           \\
			Is freely and easily available                     & X                & X                           \\
			Is supported with good teaching material           & X                &                             \\
			\hline
			Not only used in education                         &                  & X                           \\
			\hline
			Concept Coverage                                   & X                & X                           \\
			\hline
		\end{tabular}
	}
	\caption{Comparison of Scratch and Godot Orchestrator using the seventeen criteria}\label{tab:tableComparisonSummary}
\end{table}

Scratch and Godot Orchestrator employ distinct visual programming paradigms: Scratch uses block-based, while Orchestrator implements a node-based.
Scratch has a high level of abstraction which makes it good for beginners while Godot Orchestrator being a plugin for a professional grade game engine makes it better for more complex concepts.

This aligns with criteria such as \textit{“Suitable for teaching”}, \textit{“Is freely and easily available”}, and \textit{“Has a supportive user community”}, where Scratch consistently scores higher than Orchestrator in Table~\ref{tab:tableComparisonSummary}.
In contrast, Orchestrator only partially fulfills these criteria due to its reliance on prior programming knowledge and familiarity with the Godot engine.

When considering interactivity and rapid development, both environments perform well, as shown in Table~\ref{tab:tableComparisonSummary}.
Scratch allows learners to construct and test programs immediately without setup, while Orchestrator provides instant feedback within the game environment.
However, Orchestrator’s dependence on a scene context limits its ability to support isolated experimentation.

Both languages support problem decomposition and modularization, satisfying the \textit{“bite-sized chunks”} criterion.
As indicated in Table~\ref{tab:tableComparisonSummary}, Scratch achieves this through sprites and custom blocks, while Orchestrator provides more advanced structuring via functions, scripts, and scene hierarchies.

In terms of correctness, Orchestrator provides stronger support through type checking and prevention of invalid connections, whereas Scratch lacks type enforcement and detailed error feedback.
However, this support is still insufficient to fully satisfy the \textit{“Promotes writing correct programs”} criterion, as reflected in Table~\ref{tab:tableComparisonSummary}.


The two environments differ significantly in extensibility, real-world applicability, and professional relevance.
Orchestrator satisfies criteria such as \textit{“Is extensible”}, \textit{“Not only used in education”}, and \textit{“Is reliable and efficient”}, reflecting its integration within a professional development environment.
Scratch, as shown in Table~\ref{tab:tableComparisonSummary}, prioritizes accessibility and educational usability over industrial applicability.

A way to look at Godot is that the game engine is a part of a game development environment.
You would want to use other programs to create assets and then port them over to Godot.
Where as Scratch is a full game development environment, it does this by providing the tools for sprite, background and sound creation and editing.

When progressing to more advanced tools using a visual programming language **could** be more limiting than helpful.
Of the two languages Orchestrator is more suited teaching these more advanced concepts.
Because that Orchestrator is a plugin, disabling the plugin is an option, and the user would already be familiar with the keywords and features of Godot.
In the case of Scratch, users would need to switch to a new game engine, where they may be less well-prepared.
For example, Scratch does not support vectors.
Although it provides \texttt{x} and \texttt{y} values, it does not include a combined data type, unlike most modern game engines, which rely on vector-based structures.
And other game engine won’t have the same level of abstraction as Scratch.

\subsection{Surveys}\label{subsec:surveys}
The study included both a pre- and a post-survey to determine the students' assessment of their programming abilities.
The pre-survey was carried out before the workshop to determine the students' experience with programming and software design principles.
Using a Likert scale students could indicate whether they agree or disagree with statements like “I have a lot of experience in writing
computer programs”, “I have a lot of experience with visual programming languages (e.g., Scratch)”, or “I can confidently explain how events
handling can be implemented in computer games”. Note the focus on computer games in the last example. Because we used computer games to teach
software design principles, we decided to go for more games-related statements instead of generic programming-related statements. We deem this
approach defendable, as game development has served as proxy for teaching programming principles in other research projects\cite{mathrani_playit_2016,papadakis_evaluating_2020}.

A corresponding post-survey, with primarily the same questions, was performed after the workshop.
Its goal was to capture the workshop's effect on self-assessing their software design skills.
Concretely, it was used to identify differences between Scratch and Godot Orchestrator teaching software design principles.
Compared to the pre-survey, the post-survey was enriched by additional statements, to receive feedback on the usability of both tools.
Those statements were phrased like “The visual programming language helped me to understand how my game saves and updates data”.
Similarly to the pre-survey, statements were mapped to the software design concepts we wanted to convey during our workshop, as defined
in the previous section. The surveys were conducted anonymously, however it was ensured that pre- and post-survey answers could be mapped
to the same identifier. As recommend in\cite{nemoto_developing_2013}, other data collection methods (multiple-choice test, code evaluation)
have been added, to get a more complete picture of the research subject.

The question for both surveys were designed with students in mind, which would have at least a basic understanding of programming and related
concepts like variables and control structures. According to the project focus on young learners, we expected to teach either middle school or
young high school students. Thus, one challenge was to find a tone and wording that would be understandable, yet meaningful enough to describe
concepts like modularity or instantiation. A survey statement regarding modularity was phrased like this: “I can confidently explain, how a
game can be split into several parts”. While it does not mention the concept of modularity explicitly, it still conveys a similar idea using
simpler language. Other statements used the concept's name, if we deemed it understandable enough: “I can confidently explain, how programs
react to events”. As Berends~\cite{berends_survey_2006} stresses, creating questions that address the actual research goal is essential.
Therefore, we tried to focus the survey questions on the student's understanding of the earlier presented software design principles.
To achieve comparability, we included those questions both in pre- and post-survey. Apart from asking about the software design principles,
pre- and post-survey differed in the remaining question. Whereas the pre-survey collected data regarding the student's programming experience,
the post-survey included more general questions regarding usability of the respective visual programming languages.

\subsection{Educational Workshop}\label{subsec:educational-workshop}
The workshop started with us explaining the above concepts and introducing the language the group would be using.
Therefore, a 45-minute-long lecture was held, where students were involved by asking them questions and letting them ask questions.
Afterward, the students received a quick introduction into their respective game engine.
The students would then take on the role of software engineers in a game development team.
As part of the teaching materials, students were given a game design document and pre-prepared assets placed in the game.
They also received a description of the functionality their code was expected to implement.
As instructors, we stood by to help the students in case they got stuck.
This way, the students would be able to focus all their attention on writing the code, rather than understanding every aspect of the engine.
This design was chosen to account for the limited amount of available time, whilst maximizing the focus on software engineering.

The game is a simple shooter, in which the player is being chased by an ever-increasing amount of ghosts.
The player can shoot the ghosts to gain points, but the game will end if a ghost hits the player.
Further, the player can avoid the ghosts by moving left and right, or by jumping over them.
The goal is to gain as many points as possible before being hit by a ghost.

The software design concepts, that we wanted to investigate, were mapped to different features of the game to be built.
Animations for the gun are handled using a state machine (state management).
Each ghost is an instance, which makes it possible for them to spawn continuously during runtime for as long as the game goes on (instantiation).
Functions are used to organize the code and make it reusable without having to write the same lines multiple times (modularity).
Based on our testing with visual languages, we determined this to be even more important in these languages than in text-based ones.
Visual code easily covers the screen and becomes unmanageable, so organizing code into functions is crucial to be able to read it.
Both game engines allow usage of events to enable communication between scripts, so it makes sense to teach that as well.
In our game events were for example used to let ghosts know if they were hit by a bullet or to react to the player's key input (event handling).
Hence, all the earlier mentioned software design concepts could be found in our game.
The instructions, that came with the game design document and that students were encouraged to follow, tried to expose the students to problems, which could be solved by using one of the concepts.
However, the students were not obliged to use these concepts.

The project phase of the workshop was split into four blocks of around 90 minutes each.
The four blocks were distributed to two different days, as the workshop was held over two days.
On each day, we had a 30 minute-break between each block.
As Ramstetter et al.\cite{ramstetter_crucial_2010} summarizes, breaks help the children to re-gain their focus and attention.
Further, we gave them access to a whiteboard to write down their thoughts, for example when they could not instantly figure how to solve a problem with code.
To collect additional qualitative data, we took notes throughout the workshop to capture student behavior and feedback when working with the chosen visual programming languages.

\subsection{Multiple Choice Test}\label{subsec:multiple-choice-test}
After the pre- and post-surveys, students were asked to answer a multiple choice test.
In both cases the questions were the same to achieve comparability.
Since both tests included the same questions, the students were not exposed to the correct answers afterward.
The multiple choice test included ten questions, where each question had three possible answers to choose from, and
another option to indicate that the student does not know the answer.
One of the questions is shown below.
The other questions were designed in a similar way, while addressing other programming concepts too.

\begin{enumerate}
	\item You are making a game with 5 enemy types, which all move in the same way.
	      What is the best way to structure your game?
	      \begin{enumerate}[label=\Alph*.]
		      \item Copy the same movement-logic into every enemy.
		      \item Make one shared enemy behavior and use it for all enemies.
		      \item Place all enemy logic into a very large code block.
		      \item I don't know.
	      \end{enumerate}
	      \textbf{Correct answer: B}
\end{enumerate}

The overall idea was to create questions that would strike an appropriate difficulty, while also mapping to
the concepts we wanted to convey: modularity, instantiation, state management and event handling.
While the questions were the same for both tests, their order was randomized between both trials.
Having the same questions for both would allow for easy comparability.

With the help of the questions we wanted to see to what extent the visual programming languages would help improve the
student's understanding of the different concepts.

\subsection{Participant Observation \& Code Evaluation}
As surveys and tests come with their own limitations, we also observed the students and evaluted their created code.
This method has been used in other studies concerned with teaching programming concepts, like \cite{grover_assessing_2014}.
For each of them we took notes regarding how well they understood and applied the different software design principles
(modularity, instantiation, state management, instantiation).
Whereas we documented student behavior in a more unstructured
manner (i.e. quick notes on paper), we created an evaluation rubric to assess their produced code.
We came up with the
following scale to assess frequency and quality of implementation of all four design concepts.

\begin{itemize}
	\item Frequency: Always, Usually, Often, Sometimes, Never
	\item Quality: Appropriate, Somewhat Appropriate, Inappropriate
\end{itemize}